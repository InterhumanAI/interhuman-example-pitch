/** Interhuman API max upload size (for reference only; streaming has no single-file limit) */
export const MAX_UPLOAD_SIZE_MB = 32;
export const MAX_UPLOAD_SIZE_BYTES = MAX_UPLOAD_SIZE_MB * 1024 * 1024;
